{
  "name": "bisondrywall-io",
  "version": "1.0.0",
  "author": "0x6A6471 <x6a6471@fastmail.com>",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"bun --watch --outfile=dist/index.js --target=browser src/index.ts\" \"bunx http-server dist -p 3000\"",
    "build": "mkdir -p dist && cp src/index.html dist/ && bun build src/index.ts --outfile=dist/index.js --target=browser"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "http-server": "^14.1.1",
    "typescript": "^5.9.2"
  }
}
